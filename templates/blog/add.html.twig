{% extends "base.html.twig" %}

{% block content %}
	{{form_start(form)}}
        {{form_row(form.title)}}
        {{form_row(form.description, {'attr': {'placeholder': 'Ajoutez un contenu à votre article'}})}}
	{{form_end(form)}}
{% endblock %}